<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width"/>
    <style>
        body { margin: 0; color: #111723; }

        header {
            width: 100%;
            margin: 0 0 10px 0;
            background-color: #DCDCDC;
            height: 50px;
        }

        header div.logo {
            width: 600px;
            margin: 0 auto;
        }

        header div.logo img {
            padding: 7px 2px 5px;
        }

        div.content {
            margin: 40px auto 10px;
            width: 600px;
        }

        div.content div.line {
            margin: 10px 0;
            text-align: justify;
        }

        div.activate {
            margin: 20px auto 10px;
            width: 600px;
            border: 1px solid #dddddd;
            background-color: rgba(197, 255, 247, 0.2);
            border-radius: 5px;
            padding: 5px 0 10px;
        }

        div.activate div.line {
            margin: 0 10px;
        }

        div.sign {
            margin: 20px auto 10px;
            width: 600px;
            color: gray;
        }

        a {
            color: #587da1;
            text-decoration: none;
        }

        a:hover {
            color: #f53b47;
            text-decoration: none;
        }
    </style>
</head>
<body>
<header>
    <div class="logo">
        <img src="{{ base_url }}media/img/logo-horizontal.png" height="40px" />
    </div>
</header>
<div class="content">
    <div class="line"><b>Dear, {{ user.name }}!</b></div>
    <div class="line">Your account "{{ company.companyName }}" is successfully activated!</div>
    <div class="line">Information about your account:</div>
</div>
</div>
<div class="activate">
    <div class="line">Account url:</div>
    <div class="line"><a href="https://{{ company.accountName }}.{{ base_host }}/">https://{{ company.accountName }}.{{ base_host }}/</a></div>
    <div class="line">&nbsp;</div>
    <div class="line">Login:</div>
    <div class="line">{{ user.email }}</div>
    <div class="line">&nbsp;</div>
    <div class="line">Password:</div>
    <div class="line"><i>your password</i></div>
</div>
<div class="sign">
    <div class="line">Best regards,</div>
    <div class="line">InWorkFlow Team</div>
    <div class="line">&nbsp;</div>
    <div class="line"><a href="{{ base_url }}">{{ base_url }}</a></div>
    <div class="line"><a href="mailto:info@{{ base_host }}">info@{{ base_host }}</a></div>
    <div class="line"><a href="mailto:support@{{ base_host }}">support@{{ base_host }}</a></div>
</div>
</body>